Story S01: Audit dependencies and create migration plan
Status: COMPLETE
Date: 2026-02-16
Branch: revival/graveyard-hack

Deliverables:
  - MIGRATION.md: Comprehensive migration plan covering all dependency changes

Summary:
  Audited all Rust and TypeScript dependencies across the Tribeca DAO workspace.
  Documented every crate/package change needed, vipers macro replacements,
  smart-wallet stub plan, Anchor 0.22->0.30 breaking changes, current build
  errors, and the correct migration order.

Key findings:
  - 2 abandoned Rust crates (vipers, smart-wallet) requiring vendor/stub
  - 6 abandoned TypeScript packages (@saberhq/*, @gokiprotocol/client)
  - 4 transitive dependency errors blocking compilation
  - 27 #[access_control] callsites with Validate trait to migrate
  - 8 ctx.bumps.get() calls to update for Anchor 0.30
  - 9 instruction signatures with _bump params to remove
  - ~30 assert_keys_eq! macro invocations to replace
  - ~25 unwrap_int! macro invocations to replace

---

Story S02: Vendor or replace vipers crate
Status: COMPLETE
Date: 2026-02-16
Branch: revival/graveyard-hack

Deliverables:
  - Removed vipers dependency from all 3 program Cargo.toml files
  - Replaced all vipers macros with native Anchor equivalents
  - Converted all Validate trait impls to standalone validate() methods

Programs modified:
  - programs/govern/ (lib.rs, account_validators.rs, proposal.rs, Cargo.toml)
  - programs/locked-voter/ (lib.rs, state.rs, instructions/*.rs, Cargo.toml)
  - programs/simple-voter/ (lib.rs, account_validators.rs, Cargo.toml)
  - programs/whitelist-tester/ (no changes needed - did not use vipers)

Macro replacements applied:
  - assert_keys_eq!(a, b) -> require!(a.key() == b.key(), ErrorCode::KeyMismatch)
  - assert_keys_eq!(a, b, err) -> require!(a.key() == b.key(), ErrorCode::err)
  - invariant!(cond, msg) -> require!(cond, ErrorCode::InvariantFailed)
  - invariant!(cond, ErrVariant) -> require!(cond, ErrorCode::ErrVariant)
  - unwrap_int!(expr) -> expr.ok_or_else(|| error!(ErrorCode::MathOverflow))?
  - unwrap_opt!(expr, msg) -> expr.ok_or_else(|| error!(ErrorCode::UnexpectedNone))?
  - program_err!(ErrVariant) -> Err(error!(ErrorCode::ErrVariant))

New error variants added to each program's ErrorCode enum:
  - KeyMismatch, MathOverflow, InvariantFailed, UnexpectedNone, ProgramError

Validate trait migration:
  - Removed all `impl Validate for X` blocks
  - Added standalone `pub fn validate(&self) -> Result<()>` methods on each struct
  - Kept #[access_control(ctx.accounts.validate())] attributes unchanged (works with methods)

Verification:
  - cargo check --workspace: No vipers-specific errors
  - Pre-existing wasm-bindgen version error remains (unrelated, from Cargo.lock)
  - Zero references to vipers in any program source file

Next stories:
  - S03: Create smart-wallet stub crate
  - S04: Apply Anchor 0.30 API changes
  - S05: Build verification
  - S06: TypeScript SDK migration

---

Story S03: Vendor or stub smart-wallet CPI dependency
Status: COMPLETE
Date: 2026-02-16
Branch: revival/graveyard-hack

Deliverables:
  - Created programs/smart-wallet/ as a local stub crate (not a real program)
  - Eliminated crates.io dependency on abandoned smart-wallet 0.7.0
  - Eliminated transitive vipers dependency (original crate required vipers)

Files created:
  - programs/smart-wallet/Cargo.toml (name=smart-wallet, v0.7.0, anchor-lang dep only)
  - programs/smart-wallet/src/lib.rs (stub program with all needed types and CPI surface)
  - programs/smart-wallet/Xargo.toml (BPF build config)

Types stubbed (matching original crate API):
  - SmartWallet account struct (base, bump, threshold, minimum_delay, grace_period,
    owner_set_seqno, num_transactions, owners, reserved)
  - TXInstruction struct (program_id, keys, data)
  - TXAccountMeta struct (pubkey, is_signer, is_writable)
  - CreateTransaction accounts struct (smart_wallet, transaction, proposer, payer, system_program)
  - Program ID: GokivDYuQXPZCWRkwMhdH2h91KpDQXBEmpgBgs55bnpH

CPI functions stubbed (via #[program] macro auto-generation):
  - smart_wallet::cpi::create_transaction(ctx, bump, instructions)
  - smart_wallet::cpi::create_transaction_with_timelock(ctx, bump, instructions, eta)
  - smart_wallet::program::SmartWallet (program type, auto-generated)

Cargo.toml changes:
  - programs/govern/Cargo.toml: smart-wallet = { path = "../smart-wallet", features = ["cpi"] }
  - programs/simple-voter/Cargo.toml: smart-wallet = { path = "../smart-wallet", features = ["cpi"] }

Workspace inclusion:
  - Root Cargo.toml already has members = ["programs/*"], no change needed

Cargo.lock changes:
  - smart-wallet entry: removed source = "registry+..." (now local)
  - smart-wallet entry: removed vipers dependency (stub doesn't need it)

Verification:
  - cargo tree -p smart-wallet: resolves to local path, depends only on anchor-lang
  - cargo tree -p govern --depth 1: shows smart-wallet v0.7.0 (local path)
  - cargo tree -p simple-voter --depth 1: shows smart-wallet v0.7.0 (local path)
  - cargo check --workspace: No smart-wallet-specific errors
  - Pre-existing wasm-bindgen version error remains (unrelated, from Cargo.lock)

Next stories:
  - S04: Apply Anchor 0.30 API changes
  - S05: Build verification
  - S06: TypeScript SDK migration
