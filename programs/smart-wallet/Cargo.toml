[package]
name = "smart-wallet"
version = "0.7.0"
edition = "2021"
description = "Stub of Goki smart-wallet for Tribeca DAO compilation. Not a real program."

[lib]
crate-type = ["cdylib", "lib"]
name = "smart_wallet"

[features]
idl-build = ["anchor-lang/idl-build"]
no-entrypoint = []
no-idl = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
